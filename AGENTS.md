# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository with no application code, dependencies, or services yet.

- **Repository state**: Contains only `README.md` (placeholder). No programming language, framework, or package manager has been chosen.
- **No build/lint/test commands** are available until application code is added.
- **No services** need to be started.
- **No dependencies** need to be installed.

When application code is added, update this file with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious development environment caveats
